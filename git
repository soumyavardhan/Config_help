Clone <repository>
git clone <repository>

print fetch and push addresses
git remote -v

Commit changes to local repo
git commit

Push changes from branch <branch_name> to remote repo(origin)
git push origin <branch_name>

create and update to branch
git checkout -b <branch_name>
shorthand for
git branch <branch_name>
git checkout <branch_name>


switch to branch branch_name
git checkout <branch_name>

Update contents of branch
git pull

Merge branches
update to branch <branch1> in which you want to merge and then merge branch2
git checkout branch1
git merge branch2
In case of conflicts, use
git merge --abort
